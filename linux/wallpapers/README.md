# Wallpapers

Put `.jpg`, `.jpeg`, `.png`, `.webp`, or `.jxl` files here.

After installing the dotfiles, this folder is also available as
`~/Pictures/wallpapers`. Hyprpaper applies one image immediately and rotates
multiple images randomly every 30 minutes.

