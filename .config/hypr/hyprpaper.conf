# Put images in ~/Pictures/wallpapers (linked to the dotfiles/wallpapers folder).
# One image = fixed wallpaper; multiple images rotate randomly every 30 minutes.
ipc = true

wallpaper {
    monitor =
    path = ~/Pictures/wallpapers
    fit_mode = cover
    timeout = 1800
    order = random
    recursive = true
}

